<UserControl x:Class="Inventory_System.UI.Views.SIM.SIMCardType.SIMCardTypeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Inventory_System.UI.Views.SIM.SIMCardType"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                FontSize="24"
                Margin="0,5,0,0"
                Style="{StaticResource Header}"
                Text="Create type for SIM cards."/>

            <Button 
                Content="Create type"
                Margin="10,10,50,10"
                Grid.Column="1"/>
        </Grid>
        
            <DataGrid Grid.Row="1" Margin="20">

            </DataGrid>
    </Grid>
</UserControl>
