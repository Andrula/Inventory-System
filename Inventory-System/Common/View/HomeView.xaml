<UserControl x:Class="Inventory_System.Common.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:controls="clr-namespace:Inventory_System.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Persistent navigation bar -->
        <controls:NavigationBar Grid.Row="0" DataContext="{Binding NavigationBarViewModel}"/>

        <!-- Placeholder for the selected equipment view -->
        <ContentControl Grid.Row="1" Content="{Binding DynamicContent}"/>
    </Grid>
</UserControl>
